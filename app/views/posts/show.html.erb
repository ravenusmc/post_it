<div class="container">

  <h1>Showing Post Number: <%= @post.id %></h1>
  <p><%= @post.description %></p>
  <% if @post.avatar_file_name != nil %>
    <%= image_tag @post.avatar.url(:medium), class: "img-responsive" %>
  <% end %>
  
</div>